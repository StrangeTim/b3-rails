<%= render "layouts/errors", :object => @artist %>

<%= bootstrap_form_for @artist do |f| %>
  <%= f.text_field :name %>
  <%= f.text_area :bio %>
  <%= f.file_field :profile_pic %>
  <%= f.submit %>
<% end %>
